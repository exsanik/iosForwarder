{
  "name": "iosforward",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --target es2015",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^11.30.0",
    "vite": "^4.3.2"
  },
  "dependencies": {
    "spin.js": "^4.1.1",
    "vite-plugin-html": "^3.2.0"
  }
}
